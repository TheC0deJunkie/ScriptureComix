import { describe, it, expect } from 'vitest';

describe('manifest loading', () => {
  it.todo('loadManifest resolves before loadBook');
  it.todo('manifest includes book names and chapter counts');
});
