import { describe, it, expect } from 'vitest';

describe('localStorage safety', () => {
  it.todo('corrupt JSON falls back');
  it.todo('missing key returns default');
  it.todo('valid JSON parsed correctly');
});
