import { describe, it, expect } from 'vitest';

describe('loadChapterText', () => {
  it.todo('fetches per-book JSON from /data/ path');
  it.todo('returns ChapterVerse[] for valid book and chapter');
  it.todo('does not import from bible-translations-master');
});
