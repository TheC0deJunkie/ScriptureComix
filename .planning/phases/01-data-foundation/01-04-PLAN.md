---
phase: 01-data-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - components/TraditionSwitcher.tsx
  - components/BookSelector.tsx
  - App.tsx
  - tests/canons.test.ts
  - tests/nav.test.ts
autonomous: true
requirements: [NAV-01, NAV-02, DATA-04]

must_haves:
  truths:
    - "User sees a dropdown with 4 tradition options: Protestant, Catholic, Ethiopian Orthodox, Quran"
    - "Selecting a tradition changes the book list to show that tradition's books"
    - "Quran navigation uses Surah and Ayah labels instead of Book and Verse"
    - "Ethiopian Orthodox shows section headers: Old Testament, New Testament, Deuterocanonical, Additional Books"
    - "Copyright attribution is displayed in the translation picker before user selects a translation"
  artifacts:
    - path: "components/TraditionSwitcher.tsx"
      provides: "Tradition dropdown selector"
      exports: ["TraditionSwitcher"]
      min_lines: 30
    - path: "components/BookSelector.tsx"
      provides: "Manifest-driven book/chapter/verse selector with copyright display"
      exports: ["BookSelector"]
      min_lines: 50
  key_links:
    - from: "components/TraditionSwitcher.tsx"
      to: "services/types.ts"
      via: "imports Tradition, TRADITIONS, TRADITION_LABELS"
      pattern: "import.*TRADITIONS"
    - from: "components/BookSelector.tsx"
      to: "services/manifestService.ts"
      via: "imports loadManifest"
      pattern: "import.*loadManifest"
    - from: "components/BookSelector.tsx"
      to: "manifest.translations[x].copyright"
      via: "reads copyright from TranslationMeta and displays it"
      pattern: "copyright"
    - from: "App.tsx"
      to: "components/TraditionSwitcher.tsx"
      via: "renders TraditionSwitcher"
      pattern: "<TraditionSwitcher"
---

<objective>
Build the tradition switcher dropdown and manifest-driven book/chapter/verse navigation components with copyright attribution display, then wire them into App.tsx. Also implement real test assertions for canon and nav tests.

Purpose: This gives users access to all 4 scripture traditions (NAV-01) with properly labeled navigation (NAV-02) — Surah/Ayah for Quran, section-grouped books for Ethiopian Orthodox. Copyright attribution is shown in the translation picker per locked decision (DATA-04).
Output: TraditionSwitcher.tsx, BookSelector.tsx with copyright display, App.tsx integration, and real test assertions.
</objective>

<execution_context>
@C:/Users/khule/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/khule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md
@.planning/phases/01-data-foundation/01-03-SUMMARY.md

<interfaces>
From services/types.ts:
```typescript
export type Tradition = 'protestant' | 'catholic' | 'ethiopian' | 'quran';
export const TRADITION_LABELS: Record<Tradition, string> = {
  protestant: 'Protestant', catholic: 'Catholic', ethiopian: 'Ethiopian Orthodox', quran: 'Quran',
};
export const NAV_LABELS: Record<Tradition, { book: string; chapter: string; verse: string }> = {
  protestant: { book: 'Book', chapter: 'Chapter', verse: 'Verse' },
  catholic: { book: 'Book', chapter: 'Chapter', verse: 'Verse' },
  ethiopian: { book: 'Book', chapter: 'Chapter', verse: 'Verse' },
  quran: { book: 'Surah', chapter: 'Surah', verse: 'Ayah' },
};
export interface CanonManifest { tradition: Tradition; displayName: string; books: ManifestBook[]; translations: TranslationMeta[]; }
export interface ManifestBook { slug: string; displayName: string; section?: string; chapters: number[]; }
export interface TranslationMeta { id: string; displayName: string; copyright?: string; isPublicDomain: boolean; }
export const TRADITIONS: Tradition[] = ['protestant', 'catholic', 'ethiopian', 'quran'];
```

From services/manifestService.ts:
```typescript
export async function loadManifest(tradition: Tradition): Promise<CanonManifest>;
export async function loadAllManifests(): Promise<Map<Tradition, CanonManifest>>;
export function findBookSlug(manifest: CanonManifest, bookDisplayName: string): string | undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TraditionSwitcher and BookSelector components with copyright attribution</name>
  <files>components/TraditionSwitcher.tsx, components/BookSelector.tsx</files>
  <read_first>
    - services/types.ts (Tradition, TRADITIONS, TRADITION_LABELS, NAV_LABELS, CanonManifest, ManifestBook, TranslationMeta)
    - services/manifestService.ts (loadManifest, findBookSlug)
    - App.tsx (search for existing book/chapter selector UI to understand current layout patterns and styling approach — look for select elements, dropdown patterns, Tailwind classes used)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (tradition navigation model decisions, copyright attribution decision)
  </read_first>
  <action>
**1. Create `components/TraditionSwitcher.tsx`:**

```tsx
import React from 'react';
import { Tradition, TRADITIONS, TRADITION_LABELS } from '../services/types';

interface TraditionSwitcherProps {
  selected: Tradition;
  onChange: (tradition: Tradition) => void;
}

export function TraditionSwitcher({ selected, onChange }: TraditionSwitcherProps) {
  return (
    <div className="tradition-switcher">
      <label htmlFor="tradition-select" className="sr-only">Scripture Tradition</label>
      <select
        id="tradition-select"
        value={selected}
        onChange={(e) => onChange(e.target.value as Tradition)}
        // Use existing app styling patterns — inspect App.tsx for class conventions
      >
        {TRADITIONS.map((t) => (
          <option key={t} value={t}>
            {TRADITION_LABELS[t]}
          </option>
        ))}
      </select>
    </div>
  );
}
```

The select element renders 4 options: Protestant, Catholic, Ethiopian Orthodox, Quran. It is a controlled component — `selected` and `onChange` come from parent state.

**2. Create `components/BookSelector.tsx`:**

This component replaces the existing book/chapter/verse selection UI with a manifest-driven version. It also includes a translation selector with copyright attribution display.

Props:
```typescript
interface BookSelectorProps {
  tradition: Tradition;
  selectedBook: string | null;       // book slug
  selectedChapter: number | null;
  selectedTranslation: string | null; // translation id
  onBookChange: (slug: string, displayName: string) => void;
  onChapterChange: (chapter: number) => void;
  onTranslationChange: (translationId: string) => void;
}
```

Behavior:
- On mount and when `tradition` changes, call `loadManifest(tradition)` to get the book list
- Store manifest in local state via `useState` + `useEffect`
- **Translation selector with copyright attribution (DATA-04):**
  - Render a translation dropdown from `manifest.translations`
  - For each translation with a `copyright` field, display the copyright string BELOW the dropdown as a description element, visible BEFORE the user confirms selection
  - Per locked decision: "Attribution appears when user opens the translation picker, before selecting"
  - Implementation: use `<optgroup>` or a `<p>` element below the `<select>` that shows the selected translation's copyright text
  - For public domain translations (no copyright field or `isPublicDomain: true`), show "Public Domain" or nothing
  - Example rendering:
    ```tsx
    <select value={selectedTranslation} onChange={...}>
      {manifest.translations.map(t => (
        <option key={t.id} value={t.id}>
          {t.displayName}{t.copyright ? ' *' : ''}
        </option>
      ))}
    </select>
    {selectedTranslationMeta?.copyright && (
      <p className="text-xs text-gray-400 mt-1 italic">
        {selectedTranslationMeta.copyright}
      </p>
    )}
    ```
- **Book dropdown:**
  - For all traditions: render books grouped by `section` field using `<optgroup label={section}>`
  - Ethiopian Orthodox will show groups: "Old Testament", "New Testament", "Deuterocanonical", "Additional Books"
  - If no section field, render flat list
  - Label the dropdown with `NAV_LABELS[tradition].book` (shows "Surah" for Quran)
- **Chapter dropdown:**
  - When a book is selected, show chapters from 1 to `book.chapters.length`
  - Label with `NAV_LABELS[tradition].chapter`
  - For Quran: chapters.length is always 1 (each surah IS the chapter), so hide the chapter selector or auto-select chapter 1
- **Verse count display:**
  - Show "X verses" or "X ayahs" (using `NAV_LABELS[tradition].verse`) based on `book.chapters[selectedChapter - 1]`

Implementation:
```tsx
import React, { useState, useEffect } from 'react';
import { Tradition, NAV_LABELS, CanonManifest, ManifestBook, TranslationMeta } from '../services/types';
import { loadManifest } from '../services/manifestService';

interface BookSelectorProps {
  tradition: Tradition;
  selectedBook: string | null;
  selectedChapter: number | null;
  selectedTranslation: string | null;
  onBookChange: (slug: string, displayName: string) => void;
  onChapterChange: (chapter: number) => void;
  onTranslationChange: (translationId: string) => void;
}

export function BookSelector({
  tradition, selectedBook, selectedChapter, selectedTranslation,
  onBookChange, onChapterChange, onTranslationChange
}: BookSelectorProps) {
  const [manifest, setManifest] = useState<CanonManifest | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const labels = NAV_LABELS[tradition];

  useEffect(() => {
    setLoading(true);
    setError(null);
    loadManifest(tradition)
      .then(setManifest)
      .catch((err) => setError(`Failed to load ${tradition} data`))
      .finally(() => setLoading(false));
  }, [tradition]);

  if (loading) return <div>Loading...</div>;
  if (error || !manifest) return <div>{error || 'No data available'}</div>;

  const currentBook = manifest.books.find(b => b.slug === selectedBook);
  const selectedTranslationMeta = manifest.translations.find(t => t.id === selectedTranslation);

  // Group books by section
  const sections = new Map<string, ManifestBook[]>();
  for (const book of manifest.books) {
    const sec = book.section || 'Books';
    if (!sections.has(sec)) sections.set(sec, []);
    sections.get(sec)!.push(book);
  }

  const isQuran = tradition === 'quran';

  return (
    <div className="book-selector">
      {/* Translation selector with copyright attribution */}
      {manifest.translations.length > 1 && (
        <div>
          <label htmlFor="translation-select">Translation</label>
          <select
            id="translation-select"
            value={selectedTranslation || manifest.translations[0]?.id || ''}
            onChange={(e) => onTranslationChange(e.target.value)}
          >
            {manifest.translations.map(t => (
              <option key={t.id} value={t.id}>
                {t.displayName}{t.copyright ? ' *' : ''}
              </option>
            ))}
          </select>
        </div>
      )}

      {/* Copyright attribution — shown before user confirms selection (DATA-04 locked decision) */}
      {selectedTranslationMeta?.copyright && (
        <p className="text-xs text-gray-400 mt-1 italic">
          {selectedTranslationMeta.copyright}
        </p>
      )}
      {selectedTranslationMeta && !selectedTranslationMeta.copyright && selectedTranslationMeta.isPublicDomain && (
        <p className="text-xs text-gray-400 mt-1 italic">Public Domain</p>
      )}

      {/* For traditions with only 1 translation, still show copyright if present */}
      {manifest.translations.length === 1 && manifest.translations[0].copyright && (
        <p className="text-xs text-gray-400 mt-1 italic">
          {manifest.translations[0].copyright}
        </p>
      )}

      {/* Book / Surah selector */}
      <div>
        <label htmlFor="book-select">{labels.book}</label>
        <select
          id="book-select"
          value={selectedBook || ''}
          onChange={(e) => {
            const book = manifest.books.find(b => b.slug === e.target.value);
            if (book) {
              onBookChange(book.slug, book.displayName);
              // Auto-select chapter 1
              onChapterChange(1);
            }
          }}
        >
          <option value="">Select {labels.book}...</option>
          {Array.from(sections.entries()).map(([section, books]) => (
            <optgroup key={section} label={section}>
              {books.map(b => (
                <option key={b.slug} value={b.slug}>{b.displayName}</option>
              ))}
            </optgroup>
          ))}
        </select>
      </div>

      {/* Chapter selector — hidden for Quran (each surah = 1 chapter) */}
      {currentBook && !isQuran && currentBook.chapters.length > 1 && (
        <div>
          <label htmlFor="chapter-select">{labels.chapter}</label>
          <select
            id="chapter-select"
            value={selectedChapter || ''}
            onChange={(e) => onChapterChange(Number(e.target.value))}
          >
            {currentBook.chapters.map((_, i) => (
              <option key={i + 1} value={i + 1}>{i + 1}</option>
            ))}
          </select>
        </div>
      )}

      {/* Verse/Ayah count display */}
      {currentBook && selectedChapter && (
        <div className="verse-count">
          {currentBook.chapters[selectedChapter - 1]} {labels.verse.toLowerCase()}s
        </div>
      )}
    </div>
  );
}
```

Style both components to match the existing app's styling conventions (inspect App.tsx for class patterns).
  </action>
  <verify>
    <automated>cd C:/Users/khule/OneDrive/Desktop/ScriptureComix && npx tsc --noEmit components/TraditionSwitcher.tsx components/BookSelector.tsx 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - components/TraditionSwitcher.tsx contains `export function TraditionSwitcher`
    - components/TraditionSwitcher.tsx contains `TRADITIONS.map`
    - components/TraditionSwitcher.tsx contains `TRADITION_LABELS[t]`
    - components/TraditionSwitcher.tsx renders 4 options (Protestant, Catholic, Ethiopian Orthodox, Quran)
    - components/BookSelector.tsx contains `export function BookSelector`
    - components/BookSelector.tsx contains `loadManifest(tradition)`
    - components/BookSelector.tsx contains `NAV_LABELS[tradition]`
    - components/BookSelector.tsx contains `optgroup` (for section-grouped books)
    - components/BookSelector.tsx handles Quran case (hides chapter selector or auto-selects)
    - components/BookSelector.tsx contains `copyright` — reads and displays copyright from TranslationMeta
    - components/BookSelector.tsx contains `onTranslationChange` prop
    - TypeScript compilation passes for both files
  </acceptance_criteria>
  <done>TraditionSwitcher renders dropdown with 4 traditions; BookSelector loads manifest and renders section-grouped books with adapted labels (Surah/Ayah for Quran); copyright attribution displayed for copyrighted translations before selection; both compile without errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire TraditionSwitcher and BookSelector into App.tsx</name>
  <files>App.tsx</files>
  <read_first>
    - App.tsx (full file — understand current state management for book/chapter selection, existing rendering of book selector UI, version state — note: safeRead/safeWrite already added by Plan 01-03)
    - components/TraditionSwitcher.tsx (props: selected, onChange)
    - components/BookSelector.tsx (props: tradition, selectedBook, selectedChapter, selectedTranslation, onBookChange, onChapterChange, onTranslationChange)
    - services/types.ts (Tradition type)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (tradition navigation model: dropdown above book list)
  </read_first>
  <action>
**1. Add tradition state to App.tsx:**

Near existing state declarations (around line 80-100), add:
```typescript
import { Tradition } from './services/types';
import { TraditionSwitcher } from './components/TraditionSwitcher';
import { BookSelector } from './components/BookSelector';

// ... inside the component:
const [tradition, setTradition] = useState<Tradition>('protestant');
const [selectedBookSlug, setSelectedBookSlug] = useState<string | null>(null);
const [selectedTranslation, setSelectedTranslation] = useState<string | null>('kjv');
```

**2. Place TraditionSwitcher in the UI:**

Find the existing book/version selector area in App.tsx (search for the current `<select>` elements or book selection UI). Place the TraditionSwitcher ABOVE the existing book selection UI:

```tsx
<TraditionSwitcher
  selected={tradition}
  onChange={(t) => {
    setTradition(t);
    setSelectedBookSlug(null); // Reset book selection when tradition changes
    // Set default translation for the new tradition
    const defaultTranslations: Record<Tradition, string> = {
      protestant: 'kjv', catholic: 'nabre', ethiopian: 'kjv', quran: 'yusuf-ali',
    };
    setSelectedTranslation(defaultTranslations[t]);
  }}
/>
```

**3. Replace or augment the existing book selector with BookSelector:**

Below the TraditionSwitcher, render BookSelector:
```tsx
<BookSelector
  tradition={tradition}
  selectedBook={selectedBookSlug}
  selectedChapter={chapter} // use existing chapter state
  selectedTranslation={selectedTranslation}
  onBookChange={(slug, displayName) => {
    setSelectedBookSlug(slug);
    setBook(displayName); // keep existing setBook for backward compat
  }}
  onChapterChange={(ch) => {
    setChapter(ch); // use existing setChapter
  }}
  onTranslationChange={(translationId) => {
    setSelectedTranslation(translationId);
  }}
/>
```

**4. Preserve existing functionality:**

The existing `book`, `chapter`, `version` state and `loadChapterText` calls should continue to work. The BookSelector is additive — it drives the existing state through its callbacks. Do NOT remove the existing book/chapter state variables; wire the new components to update them.

**5. When tradition changes to 'quran':**
- The BookSelector handles the Quran case internally (adapted labels, no chapter sub-selector)
- Ensure `loadChapterText` or `loadChapterByTradition` is called with the correct tradition when a Quran surah is selected

**6. Keep the existing translation/version selector** — it will continue to work for Protestant tradition. For non-Protestant traditions, the translation is driven by BookSelector's translation dropdown. Optionally hide the old version selector when tradition is not 'protestant'.
  </action>
  <verify>
    <automated>cd C:/Users/khule/OneDrive/Desktop/ScriptureComix && grep -c "TraditionSwitcher\|BookSelector\|tradition.*useState" App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `import { TraditionSwitcher }` or `import { TraditionSwitcher`
    - App.tsx contains `import { BookSelector }` or `import { BookSelector`
    - App.tsx contains `useState<Tradition>('protestant')`
    - App.tsx contains `<TraditionSwitcher`
    - App.tsx contains `<BookSelector`
    - App.tsx contains `setTradition`
    - App.tsx contains `selectedTranslation` state
    - `grep -c "TraditionSwitcher\|BookSelector" App.tsx` returns >= 4 (imports + JSX usage)
  </acceptance_criteria>
  <done>App.tsx renders TraditionSwitcher above book list; BookSelector shows tradition-specific books with correct labels and copyright attribution; tradition state drives navigation; existing loadChapterText still works for Protestant; Quran shows Surah/Ayah labels</done>
</task>

<task type="auto">
  <name>Task 3: Implement real test assertions for canon and nav test files</name>
  <files>tests/canons.test.ts, tests/nav.test.ts</files>
  <read_first>
    - tests/canons.test.ts (existing it.todo stubs)
    - tests/nav.test.ts (existing it.todo stubs)
    - services/types.ts (TRADITIONS, TRADITION_LABELS, NAV_LABELS)
    - public/data/protestant/manifest.json (verify structure on disk)
    - public/data/catholic/manifest.json (verify structure on disk)
    - public/data/quran/manifest.json (verify structure on disk)
  </read_first>
  <action>
**1. Update `tests/canons.test.ts`** — replace `it.todo()` stubs with real assertions that read the manifest files from disk:

```typescript
import { describe, it, expect } from 'vitest';
import fs from 'fs';
import path from 'path';

function loadManifest(tradition: string) {
  const filePath = path.join(process.cwd(), `public/data/${tradition}/manifest.json`);
  if (!fs.existsSync(filePath)) return null;
  return JSON.parse(fs.readFileSync(filePath, 'utf-8'));
}

describe('Canon manifests', () => {
  it('protestant has exactly 66 books', () => {
    const m = loadManifest('protestant');
    expect(m).not.toBeNull();
    expect(m.books).toHaveLength(66);
    expect(m.tradition).toBe('protestant');
  });

  it('catholic has exactly 73 books', () => {
    const m = loadManifest('catholic');
    expect(m).not.toBeNull();
    expect(m.books).toHaveLength(73);
    expect(m.tradition).toBe('catholic');
    // Verify deuterocanonical books are present
    const slugs = m.books.map((b: any) => b.slug);
    expect(slugs).toContain('tobit');
    expect(slugs).toContain('judith');
    expect(slugs).toContain('wisdom');
    expect(slugs).toContain('sirach');
    expect(slugs).toContain('baruch');
    expect(slugs).toContain('1-maccabees');
    expect(slugs).toContain('2-maccabees');
  });

  it('ethiopian includes enoch and jubilees', () => {
    const m = loadManifest('ethiopian');
    expect(m).not.toBeNull();
    expect(m.tradition).toBe('ethiopian');
    const slugs = m.books.map((b: any) => b.slug);
    expect(slugs).toContain('1-enoch');
    expect(slugs).toContain('jubilees');
    // Verify Additional Books section
    const enochEntry = m.books.find((b: any) => b.slug === '1-enoch');
    expect(enochEntry.section).toBe('Additional Books');
  });

  it('quran has exactly 114 surahs', () => {
    const m = loadManifest('quran');
    expect(m).not.toBeNull();
    expect(m.books).toHaveLength(114);
    expect(m.tradition).toBe('quran');
  });

  it('quran Al-Fatiha has 6 ayahs (not 7 — Basmala excluded)', () => {
    const m = loadManifest('quran');
    expect(m).not.toBeNull();
    const fatiha = m.books[0];
    expect(fatiha.slug).toBe('al-fatiha');
    expect(fatiha.chapters[0]).toBe(6);
  });

  it('quran total ayah count is 6236', () => {
    const m = loadManifest('quran');
    expect(m).not.toBeNull();
    const total = m.books.reduce((sum: number, b: any) => sum + b.chapters[0], 0);
    expect(total).toBe(6236);
  });
});
```

**2. Update `tests/nav.test.ts`** — replace stubs with real assertions testing the type contracts:

```typescript
import { describe, it, expect } from 'vitest';
import { TRADITIONS, TRADITION_LABELS, NAV_LABELS } from '../services/types';

describe('Tradition navigation types', () => {
  it('TRADITIONS has 4 entries', () => {
    expect(TRADITIONS).toHaveLength(4);
    expect(TRADITIONS).toEqual(['protestant', 'catholic', 'ethiopian', 'quran']);
  });

  it('TRADITION_LABELS maps all traditions to display names', () => {
    expect(TRADITION_LABELS.protestant).toBe('Protestant');
    expect(TRADITION_LABELS.catholic).toBe('Catholic');
    expect(TRADITION_LABELS.ethiopian).toBe('Ethiopian Orthodox');
    expect(TRADITION_LABELS.quran).toBe('Quran');
  });

  it('Quran uses Surah/Ayah labels not Book/Verse', () => {
    expect(NAV_LABELS.quran.book).toBe('Surah');
    expect(NAV_LABELS.quran.verse).toBe('Ayah');
  });

  it('Bible traditions use Book/Chapter/Verse labels', () => {
    for (const tradition of ['protestant', 'catholic', 'ethiopian'] as const) {
      expect(NAV_LABELS[tradition].book).toBe('Book');
      expect(NAV_LABELS[tradition].chapter).toBe('Chapter');
      expect(NAV_LABELS[tradition].verse).toBe('Verse');
    }
  });
});
```
  </action>
  <verify>
    <automated>cd C:/Users/khule/OneDrive/Desktop/ScriptureComix && npx vitest run tests/canons.test.ts tests/nav.test.ts --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/canons.test.ts contains NO `it.todo(` calls (all replaced with real assertions)
    - tests/nav.test.ts contains NO `it.todo(` calls
    - `npx vitest run tests/canons.test.ts` shows all tests passing
    - `npx vitest run tests/nav.test.ts` shows all tests passing
    - Canon tests verify 66, 73, 114 book counts from manifest files
    - Nav tests verify Surah/Ayah labels for Quran
  </acceptance_criteria>
  <done>All canon and nav stub tests replaced with real assertions; protestant=66, catholic=73, quran=114, Al-Fatiha=6 ayahs verified; NAV_LABELS Quran=Surah/Ayah verified</done>
</task>

</tasks>

<verification>
- `grep "TraditionSwitcher" App.tsx` matches (component rendered)
- `grep "BookSelector" App.tsx` matches (component rendered)
- `grep "tradition.*useState" App.tsx` matches (state exists)
- `grep "copyright" components/BookSelector.tsx` matches (copyright displayed)
- TypeScript compiles without errors for components/ and App.tsx
- `npx vitest run tests/canons.test.ts tests/nav.test.ts` — all tests pass
</verification>

<success_criteria>
- Tradition switcher dropdown renders 4 options in the UI
- Selecting a tradition shows the correct book list from the manifest
- Quran uses Surah/Ayah labels instead of Book/Verse
- Ethiopian Orthodox shows section-grouped book list
- Chapter selector hidden for Quran (auto-selects chapter 1)
- Copyright attribution displayed in translation picker before user selects (DATA-04)
- Existing Protestant functionality continues to work
- All canon and nav tests pass with real assertions
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-04-SUMMARY.md`
</output>
