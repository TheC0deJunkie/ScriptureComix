---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/textLibrary.test.ts
  - tests/manifest.test.ts
  - tests/canons.test.ts
  - tests/quran.test.ts
  - tests/nav.test.ts
  - tests/storage.test.ts
  - types.ts
  - services/types.ts
autonomous: true
requirements: [DATA-03, NAV-01, NAV-02]

must_haves:
  truths:
    - "vitest runs and reports results without error"
    - "CanonManifest and ManifestBook interfaces are exported from services/types.ts"
    - "Tradition type includes protestant, catholic, ethiopian, quran"
    - "TranslationMeta interface includes copyright and isPublicDomain fields"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "jsdom"
    - path: "tests/textLibrary.test.ts"
      provides: "Stub tests for DATA-01"
    - path: "tests/canons.test.ts"
      provides: "Stub tests for CANON-01 through CANON-03"
    - path: "tests/quran.test.ts"
      provides: "Stub tests for CANON-04"
    - path: "tests/storage.test.ts"
      provides: "Stub tests for localStorage safety"
    - path: "services/types.ts"
      provides: "CanonManifest, ManifestBook, TranslationMeta, Tradition types"
      exports: ["CanonManifest", "ManifestBook", "TranslationMeta", "Tradition"]
  key_links:
    - from: "services/types.ts"
      to: "types.ts"
      via: "re-exports ChapterVerse, extends BibleVersion"
      pattern: "import.*from.*types"
---

<objective>
Set up test infrastructure (Wave 0) and define the type contracts that all subsequent plans depend on.

Purpose: Every downstream plan needs (1) a test runner to verify work and (2) shared type definitions for manifests, traditions, and translations. This plan creates both foundations.
Output: Working vitest config, stub test files for all requirements, and services/types.ts with all new interfaces.
</objective>

<execution_context>
@C:/Users/khule/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/khule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test framework and create vitest config + stub test files</name>
  <files>vitest.config.ts, tests/textLibrary.test.ts, tests/manifest.test.ts, tests/canons.test.ts, tests/quran.test.ts, tests/nav.test.ts, tests/storage.test.ts, package.json</files>
  <read_first>
    - package.json (current dependencies)
    - vite.config.ts (current Vite config to align vitest config)
    - .planning/phases/01-data-foundation/01-VALIDATION.md (Wave 0 requirements and test map)
  </read_first>
  <action>
1. Install test dependencies:
   ```
   npm install -D vitest @vitest/ui jsdom @testing-library/react @testing-library/jest-dom
   ```

2. Create `vitest.config.ts` at project root:
   ```typescript
   import { defineConfig } from 'vitest/config';
   import react from '@vitejs/plugin-react';
   import path from 'path';

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: 'jsdom',
       globals: true,
       setupFiles: [],
       include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, '.'),
       },
     },
   });
   ```

3. Create `tests/textLibrary.test.ts` with stub tests:
   - `describe("loadChapterText")` with `it.todo("fetches per-book JSON from /data/ path")`
   - `it.todo("returns ChapterVerse[] for valid book and chapter")`
   - `it.todo("does not import from bible-translations-master")`

4. Create `tests/manifest.test.ts` with stub tests:
   - `it.todo("loadManifest resolves before loadBook")`
   - `it.todo("manifest includes book names and chapter counts")`

5. Create `tests/canons.test.ts` with stub tests:
   - `it.todo("protestant 66 books")` — verifies protestant manifest has exactly 66 books
   - `it.todo("catholic 73 books")` — verifies catholic manifest has exactly 73 books
   - `it.todo("ethiopian enoch jubilees")` — verifies ethiopian manifest includes enoch and jubilees

6. Create `tests/quran.test.ts` with stub tests:
   - `it.todo("fatiha numbering")` — Al-Fatiha verse 1 is NOT Basmala
   - `it.todo("total ayahs 6236")` — total ayah count across 114 surahs is 6236

7. Create `tests/nav.test.ts` with stub tests:
   - `it.todo("tradition switcher")` — renders 4 tradition options
   - `it.todo("quran labels")` — Quran uses Surah/Ayah labels not Book/Verse

8. Create `tests/storage.test.ts` with stub tests:
   - `it.todo("corrupt JSON falls back")` — safeRead returns default on corrupt data
   - `it.todo("missing key returns default")` — safeRead returns default when key absent
   - `it.todo("valid JSON parsed correctly")` — safeRead returns parsed value

Each test file imports from `vitest` (`describe`, `it`, `expect`). Stubs use `it.todo()` so they are tracked but do not fail.
  </action>
  <verify>
    <automated>cd C:/Users/khule/OneDrive/Desktop/ScriptureComix && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts contains `environment: 'jsdom'`
    - vitest.config.ts contains `include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx']`
    - tests/textLibrary.test.ts contains `describe("loadChapterText"`
    - tests/canons.test.ts contains `it.todo("protestant 66 books")`
    - tests/canons.test.ts contains `it.todo("catholic 73 books")`
    - tests/quran.test.ts contains `it.todo("fatiha numbering")`
    - tests/storage.test.ts contains `it.todo("corrupt JSON falls back")`
    - `npx vitest run` exits 0 (all todos are pending, none fail)
  </acceptance_criteria>
  <done>vitest runs successfully with all stub tests in todo state; no failures</done>
</task>

<task type="auto">
  <name>Task 2: Define type contracts for traditions, manifests, and translations</name>
  <files>services/types.ts, types.ts</files>
  <read_first>
    - types.ts (existing types: BibleVersion, ChapterVerse, TextCatalogEntry, BOOK_COLLECTIONS)
    - services/textLibrary.ts (existing ScriptureEntry interface)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (CanonManifest, ManifestBook, TranslationMeta schemas)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (tradition navigation model decisions)
  </read_first>
  <action>
1. Create `services/types.ts` with the following type definitions:

```typescript
// Tradition identifiers — the four top-level canon groupings
export type Tradition = 'protestant' | 'catholic' | 'ethiopian' | 'quran';

// Display labels per tradition
export const TRADITION_LABELS: Record<Tradition, string> = {
  protestant: 'Protestant',
  catholic: 'Catholic',
  ethiopian: 'Ethiopian Orthodox',
  quran: 'Quran',
};

// Labels for the 3-level navigation per tradition
export const NAV_LABELS: Record<Tradition, { book: string; chapter: string; verse: string }> = {
  protestant: { book: 'Book', chapter: 'Chapter', verse: 'Verse' },
  catholic: { book: 'Book', chapter: 'Chapter', verse: 'Verse' },
  ethiopian: { book: 'Book', chapter: 'Chapter', verse: 'Verse' },
  quran: { book: 'Surah', chapter: 'Surah', verse: 'Ayah' },
};

// Canon manifest — loaded eagerly, one per tradition
export interface CanonManifest {
  tradition: Tradition;
  displayName: string;
  books: ManifestBook[];
  translations: TranslationMeta[];
}

// Single book entry in the manifest
export interface ManifestBook {
  slug: string;           // kebab-case file name, e.g. "genesis", "al-fatiha"
  displayName: string;    // human-readable, e.g. "Genesis", "Al-Fatiha"
  section?: string;       // e.g. "Old Testament", "Deuterocanonical", "Additional Books"
  chapters: number[];     // verse count per chapter; index 0 = chapter 1
}

// Translation metadata shown in picker
export interface TranslationMeta {
  id: string;             // "kjv", "niv", "nabre", "yusuf-ali"
  displayName: string;    // "King James Version"
  copyright?: string;     // attribution string shown before selection (null for PD)
  isPublicDomain: boolean;
}

// All traditions in order for the switcher dropdown
export const TRADITIONS: Tradition[] = ['protestant', 'catholic', 'ethiopian', 'quran'];
```

2. In `types.ts`, add the following AFTER existing exports (do NOT remove any existing types):
   - Add `export { type Tradition, type CanonManifest, type ManifestBook, type TranslationMeta } from './services/types';`
   - This re-export keeps types.ts as the single import point for components that already import from it

3. Do NOT modify or remove:
   - BibleVersion enum (still used by existing code until refactored in Plan 03)
   - BOOK_COLLECTIONS (still used until refactored in Plan 04)
   - ChapterVerse, TextCatalogEntry, ChapterTextResult (kept as-is)
  </action>
  <verify>
    <automated>cd C:/Users/khule/OneDrive/Desktop/ScriptureComix && npx tsc --noEmit services/types.ts 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - services/types.ts contains `export type Tradition = 'protestant' | 'catholic' | 'ethiopian' | 'quran'`
    - services/types.ts contains `export interface CanonManifest`
    - services/types.ts contains `export interface ManifestBook`
    - services/types.ts contains `export interface TranslationMeta`
    - services/types.ts contains `isPublicDomain: boolean`
    - services/types.ts contains `copyright?: string`
    - services/types.ts contains `export const TRADITIONS: Tradition[]`
    - services/types.ts contains `export const NAV_LABELS`
    - types.ts contains `export { type Tradition` or `export type { Tradition`
    - TypeScript compilation has no errors for services/types.ts
  </acceptance_criteria>
  <done>services/types.ts exports CanonManifest, ManifestBook, TranslationMeta, Tradition, TRADITIONS, NAV_LABELS; types.ts re-exports them; tsc --noEmit passes</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=dot` exits 0 with all stubs in todo state
- `npx tsc --noEmit services/types.ts` passes with no errors
- `grep -c "export interface" services/types.ts` returns at least 3 (CanonManifest, ManifestBook, TranslationMeta)
</verification>

<success_criteria>
- Test runner works: `npx vitest run` exits 0
- All 7 test stub files exist with requirement-mapped todo tests
- services/types.ts defines the type contracts for all downstream plans
- types.ts re-exports new types for component consumption
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
